cmake_minimum_required(VERSION 2.8)
project(tanchishe)
find_package(OpenCV 4.7 REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(tanchishe tanchishe.cpp)
target_link_libraries(tanchishe ${OpenCV_LIBS})
